import React from "react";

const SearchForServices = props => {
  const services = props.services;
  const selectedServices = props.selectedServices;

  return (
    <div>
      <h4>Search for Services</h4>
      <br />
    </div>
  );
};

export default SearchForServices;
